<resources>
    <!-- Base application theme. -->
    <style name="Theme.RatingsApp" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->

        <!-- Status bar color. -->
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.RatingsApp.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.RatingsApp.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.RatingsApp.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>